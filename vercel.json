{
  "version": 2,
  "buildCommand": "cd front && npm install && npm run build && cd .. && npm install",
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.js"
    }
  ]
}
